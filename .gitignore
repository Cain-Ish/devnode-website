node_modules/
dist/
*.local

# The CV carries a phone number and full employment history. It is uploaded to
# the host at /cv/mariusz-machuta-cv.pdf; the page only needs that path.
public/cv/

# Design reference bundle from Claude Design — kept locally, never committed.
design_handoff_devnode_landing/
